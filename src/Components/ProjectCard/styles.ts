import styled from "styled-components";

export const Tag = styled.div`
    display: flex;
    gap: 8px;
    background-color: #0D6EFD;
    padding: 8px;
    border-radius: 8px;
    color: white;

`

export const TagsView = styled.div`
    width: "auto";
    gap: 8px;
    display: flex;
    flex-direction: row;
    padding: 8px;
    flex-wrap: wrap;
    white-space: nowrap;
`

export const TimeProject = styled.div`
    width: 100%;
    display: flex;
    justify-content: space-between;
    padding: 8px;
`
export const ContainerDateProject = styled.div`
    display: flex;
    flex-direction: column;
`

export const LabelProject = styled.div`
    font-size: 12px;
    font-family: Arial, Helvetica, sans-serif;
`

export const DateProject = styled.div`
    display: flex;
    font-family: Arial, Helvetica, sans-serif;
    
`

export const CentralizeButton = styled.div`
    display: flex;
    justify-content: center;
    align-items: center;
    margin-top: 8px;
`
